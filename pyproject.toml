[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pip-andy"
version = "1.0.0"
description = "Package Python buatan sendiri"
readme = "README.md"
requires-python = ">=3.8"
authors = [
  { name = "Andy" }
]

[project.urls]
Homepage = "https://github.com/username/pip-andy"